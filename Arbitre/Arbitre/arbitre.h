#pragma once
#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>

std::vector<int> infoMap(const char * mapName);
bool checkSolution(const char* solutionName);
bool checkTest();